{
  "name": "bun",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "benchmark": "bun run index.ts",
    "clean": "rimraf results.json"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "common": "workspace:*"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
